# -----------------------------------------------------------------------------
#   - invoked on push, pull_request, manual trigger, or schedule
#   - test under at least 3 versions of python
# -----------------------------------------------------------------------------
name: build
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * *"
  
jobs:
  build:
    uses: 
      ihmeuw/vivarium_build_utils/.github/workflows/build.yml@mainalbrja/mic-5469/modularize-workflows
    with:
      python_version: "3.9,3.10,3.11"
      dependencies: "layered_config_tree"
      skip_mypy: false
